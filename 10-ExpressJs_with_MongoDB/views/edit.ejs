<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
</head>
<body>
    <h3>Edit chat : <%= chat._id  %></h3>
    <p>This chat was sent from <b><%= chat.from %></b> to <b><%= chat.to %></b></p>
    <form action="/chats/<%= chat._id %>?method=PUT" method="post">
        <textarea name="msg" rows="8", cols="40"><%= chat.msg %></textarea><br>
        <button>Edit</button>
    </form>
</body>
</html>